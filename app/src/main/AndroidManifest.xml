<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kmm.mrfever">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChildProfileCreatedActivity"></activity>
        <activity android:name=".YourChildrenActivity" />
        <activity android:name=".AddChildActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".DoctorsActivity" />
        <activity android:name=".ChildrenActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".UserCreatedActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IndexActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>